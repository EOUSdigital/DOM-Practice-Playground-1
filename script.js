//TODO: Here’s what I’m giving you right now:

//? 1. a one-page DOM 80/20 cheat sheet (the minimum you need, clearly grouped)
//  → [Download DOM 80/20 Cheat Sheet — Lesson 01](sandbox:/mnt/data/DOM_80-20_CheatSheet_Lesson01.md)

//? 2. a 12-line “DOM kata” you can type once a day (2–3 minutes). It drills the same core moves until they’re automatic:
//  select → traverse → create → insert → edit → remove.

//? 3. a micro-routine (10 minutes max) to actually retain this:

//* • Minute 0–1: open Console, paste this to “warm up”:

(() => { console.clear();
    console.log('URL:', document.URL);
    console.log('Title:', document.title);
    console.log('Links:', document.links.length);
    console.log('Images:', document.images.length);
})();

//* • Minutes 1–4: type today’s DOM kata from the cheat sheet (no copy/paste if possible).
//* • Minutes 4–8: pick any 2 from these:
//      • change a class on the next sibling of the first `<p>`
//      • append a new `<li>` to any list on the page
//      • add `data-index` to all `<a>` in the first `<nav>`
//      • clone something you created and insert it before the original
//* Minutes 8–10: answer these out loud without looking:
//      • how do you get the first match? (→ `querySelector`)
//      • how do you add text safely? (→ `textContent`)
//      • how do you insert at the start of a container? (→ `prepend`)
//      • how do you get the next element sibling? (→ `nextElementSibling`)
//      • does `append(existingNode)` copy or move? (→ moves)

//  If you do that routine three days in a row, you’ll feel the fog lift. Promise.

//? Day 1






















